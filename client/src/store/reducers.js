import * as actions from './types';

const initial = {
	app: {
	}
};

const app = (state = initial.app, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default { app };
